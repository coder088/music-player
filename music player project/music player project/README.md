## Getting Started

Welcome to the VS Code Java world. Here is a guideline to help you get started to write Java code in Visual Studio Code.

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
- `lib`: the folder to maintain dependencies

Meanwhile, the compiled output files will be generated in the `bin` folder by default.

> If you want to customize the folder structure, open `.vscode/settings.json` and update the related settings there.

## Dependency Management

The `JAVA PROJECTS` view allows you to manage your dependencies. More details can be found [here](https://github.com/microsoft/vscode-java-dependency#manage-dependencies).

#Questo progetto è un music player che permette di leggere file musicali inserendoli nella cartella src emodificando la stringa filepath con il nome del file. Eseguendo il programma si ha accesso a funzioni per fermare, resettare o far partire la musica oltre a due opzioni per alzare il volume.
Per realizzarlo non è stato fatto uso di intelligenza artificiale se non per aiutarmi nell'apprendimento del funzionamento della libreria javax.sound.sampled.FloatControl
considerando la scarsa quantità di risorse che ho trovato su internet. Il progetto è stato realizzato interamente da me nell'arco di una settimana.
